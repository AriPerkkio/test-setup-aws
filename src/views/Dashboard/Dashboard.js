import React from 'react';

const BASE_CLASS = 'dashboard';

const DashBoard = () => {
    return (
        <div className={BASE_CLASS}>
            Dashboard
        </div>
    );
};

export default DashBoard;